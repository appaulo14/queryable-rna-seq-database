function set_qtip(element_name,title,text,position){
  <% I18n.with_locale('en.help_tips.help_tips') do %>
    <% debugger %>
  <% end %>
  var position_args;
  if (position == 'above'){
    position_args = {
      my: 'bottom center',
      at: 'top center' 
    };
  }
  else{
    position_args = {
      my: 'left center',
      at: 'right center' 
    };
  }
   
   $('#' + element)
          .removeData('qtip') 
          .qtip({
                  content: {
                          text: text, 
                          title: {
                                  text: title,
                                  button: true
                          }
                  },
                  position: position_args,
                  show: {
                          event: 'click', 
                          ready: false
                  },
                  hide: false,
                  style: {
                          classes: 'qtip-shadow qtip-' + 'light'
                  }
          });
  }
