<%= fields_for object do |qde| %>
  <%= qde.label :sample_comparison_id, "Samples to compare" %>
  <%= qde.select :sample_comparison_id, 
    options_for_select(qde.object.available_sample_comparisons,
      qde.object.sample_comparison_id)  %> 
<% end %>
