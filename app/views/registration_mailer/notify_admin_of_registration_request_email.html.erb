<!DOCTYPE html>
<html>
  <head>
    <title>New User Registration Request Received</title>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>New User Registration Request Received for user: <%= @user.name %></h1>
    <table border="1">
      <tr><th>Id</th><td><%= @user.id %></td></tr>
      <tr><th>Name</th><td><%= @user.name %></td></tr>
      <tr><th>Email Address</th><td><%= @user.email %></td></tr>
      <tr><th>Why they want to use the system</th><td><%= @user.description %></td></tr>
    </table>
    <br/>
    <div>
      <%= link_to('Go to confirm user page',
                  "#{@base_url}/admin/confirm_user?user_id=#{@user.id}") %>
    </div>
    <div>
      <%= link_to('Go to delete user page',
                  "#{@base_url}/admin/delete_unconfirmed_user?user_id=#{@user.id}") %>
    </div>
  </body>
</html>
